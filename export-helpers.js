// ============================================================
// EXPORT HELPERS - Used by renderer for HTML export
// ============================================================

(function() {
    'use strict';

    // These functions are defined within the renderer's export code,
    // but we need to ensure the exportElementToHTML function is available
    // for the buildExportHTML function.

    // The exportElementToHTML function is already defined in renderer.js
    // as part of the export functionality. This file serves as a placeholder
    // to document the export helpers.

    console.log('Export helpers available via renderer.js');

})();